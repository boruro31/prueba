<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Extensión del formulario de factura para agregar botón de cancelar -->
    <record id="view_account_move_form_inherit_cancel_reason" model="ir.ui.view">
        <field name="name">account.move.form.cancel.reason</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Agregamos un grupo al final del form con el botón -->
            <xpath expr="//sheet" position="inside">
                <group>
                    <button name="button_open_cancel_wizard"
                            type="object"
                            string="Seleccionar Motivo de Cancelación"
                            class="btn-primary"
                            t-if="record.state.raw_value == 'cancel'"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
